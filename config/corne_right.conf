# TEMPORARY DIAGNOSTIC — remove after confirming the right-half thumb-key issue.
# Enables USB CDC-ACM serial logging so kscan/matrix events can be observed
# when the right half is plugged in via USB. Applies to the corne_right build only.
CONFIG_ZMK_USB_LOGGING=y
